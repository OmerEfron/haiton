---
name: close-feature
description: Close a planned feature after its goal test passes. Use only when the user runs /close-feature.
---

Run `/close-feature` (optionally with a feature id). Requires the feature goal test to pass. Does not start another feature.
